Read me.

TODO:

- Redo layout, probably with nested LinearLayouts instead of GridLayout, for scaling to other sizes than 480x800 hdpi.
- Start up with most recently used task list.
- Fix other buttons
